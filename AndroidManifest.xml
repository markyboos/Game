<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.game.thrones"
      android:versionCode="8"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="27"/>
    <application android:label="@string/app_name" android:icon="@mipmap/ic_launcher">
        <activity android:name="MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        
        <activity android:name=".activity.PlayerActionActivity"/>
        <activity android:name=".activity.MapCanvasActivity"/>
        <activity android:name=".activity.InventoryActivity"/>
        <!--<activity android:name=".activity.HeroViewActivity"/>-->
    </application>
</manifest>
